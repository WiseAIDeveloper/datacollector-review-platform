{
  "name": "datacollector-review-platform",
  "private": true,
  "scripts": {
    "format": "prettier --write 'web/**/*.{html,js,css}'",
    "format:check": "prettier --check 'web/**/*.{html,js,css}'",
    "test:frontend": "node scripts/check_frontend.cjs"
  },
  "devDependencies": {
    "@babel/parser": "7.28.4",
    "prettier": "3.6.2"
  }
}
